name: Payment Service CI

on:
  push:
    branches:
      - main
      - master
    paths:
      - "payment-service/payment-service/**"
      - ".github/workflows/payment-service.yml"
  pull_request:
    paths:
      - "payment-service/payment-service/**"
      - ".github/workflows/payment-service.yml"
  workflow_dispatch:

jobs:
  test:
    name: Test payment-service
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: payment-service/payment-service

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: payment-service/payment-service/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
